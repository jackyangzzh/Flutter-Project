import 'package:flutter/material.dart';

class FavoriateScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Text('So empty right now -- Add some favoriates!'),);
  }
}
